---
import "./sectionTitle.scss";

interface Props {
  title: string;
  className?: string;
  classNameTitle?: string;
}

const { title, className, classNameTitle } = Astro.props;
---

<div class=`${className} block-header`>
  <h2 class=`${classNameTitle} title2 block-title`>{title}</h2>
</div>
